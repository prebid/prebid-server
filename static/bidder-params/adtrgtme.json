{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Adtrgtme adapter params",
  "description": "A schema which validates params accepted by the Adtrgtme adapter",
  "type": "object",

  "properties": {
    "site_id": {
      "type": "integer",
      "description": "Site ID"
    }
  },

  "required": ["site_id"]
}