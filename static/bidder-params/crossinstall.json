{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "CrossInstall Adapter Params",
  "description": "A schema which validates params accepted by the CrossInstall adapter",
  "type": "object",
  "properties": {
    "reward": {
      "type": "integer",
      "description": "An integer defining the reward (skip) from CrossInstall"
    },
    "region": {
      "type": "string",
      "description": "A string defining region for choosing the closest CrossInstall endpoint"
    },
    "mraid_supported": {
      "type": "boolean",
      "description": "A bool config to determine if MRAID is supported by CrossInstall"
    }
  }
}
