endpoint: "https://{{.ZoneID}}.videostep.com/bid/ServerBidAdContent"
maintainer:
  email: "system_operations@invibes.com"
gvlVendorID: 436
capabilities:
  site:
    mediaTypes:
      - banner
userSync:
  # invibes supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - iframe
endpointCompression: "GZIP"