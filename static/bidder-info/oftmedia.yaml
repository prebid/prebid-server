endpoint: "http://ghb.ortb.152media.info/pbs/ortb"
maintainer:
  email: "admin@152media.com"
gvlVendorID: 1111
capabilities:
  app:
    mediaTypes:
      - banner
      - video
  site:
    mediaTypes:
      - banner
      - video
userSync:
  # oftmedia supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - iframe
