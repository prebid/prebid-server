endpoint: "https://direct.adsrvr.org/bid/bidder/{{.SupplyId}}"
maintainer:
  email: "Prebid-Maintainers@thetradedesk.com"
gvlVendorID: 21
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  # User sync is supported but it requires configuration. Reach out to a TAM to get a <CookiePartnerId>
  # Here is an example of how the url should look like:
  # https://match.adsrvr.org/track/rid?ttd_pid=<CookiePartnerId>&fmt=json&gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&us_privacy={{.USPrivacy}}&redirect={{.RedirectURL}}
openrtb:
  gpp-supported: true
