endpoint: "https://pbs.aralego.com/prebid"
maintainer:
  email: "support@ucfunnel.com"
gvlVendorID: 607
capabilities:
  app:
    mediaTypes:
      - banner
      - video
  site:
    mediaTypes:
      - banner
      - video
userSync:
  redirect:
  # for syncing to work, please register your Prebid Server domain with the contact email above
    url: "https://sync.aralego.com/idsync?gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&usprivacy={{.USPrivacy}}&redirect={{.RedirectURL}}"
    userMacro: "SspCookieUserId"
