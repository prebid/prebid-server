endpoint: "http://prebid-{{.Host}}.xe.works/?pid={{.SourceId}}"
maintainer:
  email: "team@xe.works"
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  # xe.works supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect