endpoint: "https://delivery.clickonometrics.pl/ortb/prebid/bid"
maintainer:
  email: "it@clickonometrics.pl"
gvlVendorID: 773
capabilities:
  site:
    mediaTypes:
      - banner
      - video
userSync:
  # Clickonometrics supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect
