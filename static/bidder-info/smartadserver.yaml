maintainer:
  email: "support@smartadserver.com"
gvlVendorID: 45
capabilities:
  app:
    mediaTypes:
      - banner
      - video
  site:
    mediaTypes:
      - banner
      - video
userSync:
  redirect:
    url: "https://ssbsync-global.smartadserver.com/api/sync?callerId=50&gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&us_privacy={{.USPrivacy}}&redirectUri={{.RedirectURL}}"
    userMacro: "[ssb_sync_pid]"
