endpoint: "https://grid.bidswitch.net/sp_bid?sp=prebid"
maintainer:
  email: "grid-tech@themediagrid.com"
gvlVendorID: 686
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  redirect:
    url: "https://x.bidswitch.net/check_uuid/{{.RedirectURL}}?gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&gpp={{.GPP}}&gpp_sid={{.GPPSID}}&us_privacy={{.USPrivacy}}"
    userMacro: "${BSW_UUID}"
