endpoint: "http://ghb.spotim.market/pbs/ortb"
maintainer:
  email: "monetization@openweb.com"
gvlVendorID: 280
capabilities:
  app:
    mediaTypes:
      - banner
      - video
  site:
    mediaTypes:
      - banner
      - video
userSync:
  # openweb supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect
