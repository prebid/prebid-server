endpoint: "http://{{.Host}}"
endpointCompression: gzip
maintainer:
  email: "abdulbaki.cam@admatic.com.tr"
gvlVendorID: 1281
modifyingVastXmlAllowed: true
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  # adtarget supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - iframe
