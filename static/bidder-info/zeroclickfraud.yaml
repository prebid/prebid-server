endpoint: "http://{{.Host}}/openrtb2?sid={{.SourceId}}"
maintainer:
  email: "support@datablocks.net"
capabilities:
  app:
    mediaTypes:
      - banner
      - native
      - video
  site:
    mediaTypes:
      - banner
      - native
      - video
userSync:
  iframe:
    url: "https://s.0cf.io/sync?gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&us_privacy={{.USPrivacy}}&r={{.RedirectURL}}"
    userMacro: "${uid}"