endpoint: "https://prebid.v.fwmrm.net/ortb/ssp"
endpointCompression: gzip
maintainer:
  email: prebid-maintainer@freewheel.com
gvlVendorID: 285
modifyingVastXmlAllowed: true
openrtb:
  gpp-supported: true
capabilities:
  app:
    mediaTypes:
      - video
  site:
    mediaTypes:
      - video
userSync:
  iframe:
    url: "https://user-sync.fwmrm.net/ad/u?mode=pbs-user-sync&gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&us_privacy={{.USPrivacy}}&pbs_redirect={{.RedirectURL}}"
    userMacro: "#{user.id}"