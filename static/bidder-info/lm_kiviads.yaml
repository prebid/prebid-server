endpoint: "http://pbs.kiviads.live/?pid={{.SourceId}}&host={{.Host}}"
maintainer:
  email: "prebid@kiviads.com"
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  # lmkiviads supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect