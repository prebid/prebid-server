maintainer:
  email: "support@vrtcal.com"
capabilities:
  app:
    mediaTypes:
      - banner
userSync:
  redirect:
    # vrtcal supports user syncing, but requires configuration by the host. paste the
    # url provided by the bidder below to enable.
    url: ""