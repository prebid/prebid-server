endpoint: "https://srv.aso1.net/pbs/bidder?zid={{.ZoneID}}"
maintainer:
  email: "support@adsrv.org"

capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  supports:
    redirect:
      url: "https://track.aso1.net/sync/v2?gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&usp={{.USPrivacy}}&gpp={{.GPP}}&gpp_sid={{.GPPSID}}&redir={{.RedirectURL}}"
      userMacro: "{uid}"
