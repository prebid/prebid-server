maintainer:
  email: "none"
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  # facebook's audience network supports user syncing, but requires configuration by the host.
  supports:
    - redirect