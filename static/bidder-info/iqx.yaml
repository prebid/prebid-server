endpoint: "http://localhost:1337/?pid={{.SourceId}}&host={{.Host}}&pbs=1"
maintainer:
  email: "it@iqzone.com"
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - native
  site:
    mediaTypes:
      - banner
      - video
      - native
userSync:
  # IQX supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect