maintainer:
  email: "prebid@triplelift.com"
gvlVendorID: 28
capabilities:
  app:
    mediaTypes:
      - native
  site:
    mediaTypes:
      - native
userSync:
  # Triplelift supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  # 
  # Example redirect / iframe:
  #   url: "https://eb2.3lift.com/[getuid/sync]?gdpr={{.GDPR}}&cmp_cs={{.GDPRConsent}}&us_privacy={{.USPrivacy}}&redir={{.RedirectURL}}"
  #   userMacro: "$UID"
  # 
  # RedirectURL domain must be allowlisted by Triplelift
  supports:
    - redirect
    - iframe

