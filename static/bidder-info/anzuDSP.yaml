aliasOf: "xeworks"
endpoint: "https://rtb.anzu-rtb.live/?pid={{.SourceId}}&host={{.Host}}&pbs=1"
maintainer:
  email: "prebid@anzu.io"
userSync:
  # anzuDSP supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect
