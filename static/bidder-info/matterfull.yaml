aliasOf: "xeworks"
endpoint: "https://rtb.mtflll.com?pid={{.SourceId}}&host={{.Host}}&pbs=1"
maintainer:
  email: "adops@bematterfull.com"
userSync:
  # matterfull supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect
