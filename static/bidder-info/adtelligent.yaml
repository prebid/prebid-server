endpoint: "http://ghb.adtelligent.com/pbs/ortb"
maintainer:
  email: "hb@adtelligent.com"
gvlVendorID: 410
capabilities:
  app:
    mediaTypes:
      - banner
      - video
  site:
    mediaTypes:
      - banner
      - video
userSync:
  # adtelligent supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - iframe
