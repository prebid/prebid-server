endpoint: "https://dmx-direct.districtm.io/b/v2"
maintainer:
  email: "prebid@districtm.net"
gvlVendorID: 144
capabilities:
  site:
    mediaTypes:
      - banner
      - video
  app:
    mediaTypes:
      - banner
      - video
userSync:
  # dmx supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect
